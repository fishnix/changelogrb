input {
  redis {
    host => "queue"
    type => "redis"
    data_type => "list"
    key => "changelogrb"
  }
}
 
output {
  stdout { 
    codec => rubydebug
  }
  elasticsearch {
    embedded => true
  }
}
